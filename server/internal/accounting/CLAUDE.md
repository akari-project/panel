# accounting：流量入账

规格：spec/22。

- ACC-03 Lua 脚本是唯一入账路径：去重、倍率、累加、脏集合、超额判定一次完成。
- ACC-05 worker 以 `RENAME` 摘取脏集合，批次 ID 幂等。
- ACC-06 用量键带周期编号，重置即切换编号，不清零。
- ACC-08–11 配额租约。
- 任何改动运行 `make chaos`：落库用量必须等于节点计数总和。
